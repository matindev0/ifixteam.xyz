import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'xyz.ifixteam.convertwebsitetoapp',
  appName: 'iFix',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;